.navbar {
    z-index: 1;
}

.navbar-brand {
    padding-top: 24px
    img {
        max-width: 134px;
    }
}

.navbar-nav > li > a {
    // color: #fff;
    font-family: $font-oswald;
    font-size: 20px;
    font-weight: 200;
    text-transform: uppercase;
}
